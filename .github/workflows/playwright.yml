name: Playwright Tests

on:
  push:
    branches: [main]
    paths:
      - "Sources/MarkViewCore/**"
      - "Tests/playwright/**"
      - "Tests/TestRunner/Fixtures/**"
  pull_request:
    branches: [main]
    # Same path filters as push — skips docs/npm-only PRs. Safe to filter:
    # this is NOT a required status check (required: Build & Test, Golden Drift Check),
    # so a skipped run cannot block auto-merge.
    paths:
      - "Sources/MarkViewCore/**"
      - "Tests/playwright/**"
      - "Tests/TestRunner/Fixtures/**"

# Cancel superseded runs for the same branch/PR
concurrency:
  group: playwright-${{ github.ref }}
  cancel-in-progress: true

jobs:
  playwright:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          # Key on the lockfile (exact resolved versions), not package.json.
          cache-dependency-path: Tests/playwright/package-lock.json

      - name: Install dependencies
        working-directory: Tests/playwright
        run: npm ci

      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('Tests/playwright/package-lock.json') }}

      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        working-directory: Tests/playwright
        run: npx playwright install chromium --with-deps

      - name: Install Playwright OS deps (browser cache hit)
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        working-directory: Tests/playwright
        run: npx playwright install-deps chromium

      - name: Check fixtures are committed
        run: |
          if [[ ! -f "Tests/playwright/fixtures/golden-corpus.html" ]]; then
            echo "ERROR: Playwright fixtures missing. Run: bash scripts/gen-playwright-fixtures.sh"
            echo "Fixtures must be committed to Tests/playwright/fixtures/"
            exit 1
          fi
          echo "Fixtures present: $(ls Tests/playwright/fixtures/*.html | wc -l | tr -d ' ') files"

      - name: Run Playwright tests
        working-directory: Tests/playwright
        run: npx playwright test
        env:
          CI: true

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: Tests/playwright/playwright-report/
          retention-days: 7
