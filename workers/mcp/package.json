{
  "name": "markview-mcp-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker — MCP HTTP endpoint for MarkView (Smithery capability listing)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250401.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.65.0"
  }
}
